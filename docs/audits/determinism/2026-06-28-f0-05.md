# Auditoria de determinismo — F0-05

Data: 2026-06-28  
Escopo: diff atual de `F0-05`, estado `wwiirun.run-state.v1` e hash `fnv1a64-v1`  
Veredito: `Pass`

## Ambiente

- Node `v24.15.0`;
- npm `11.12.1`;
- Vitest `4.1.9`;
- Playwright `1.61.1`, Chromium gerenciado pelo projeto.

## Superfície determinística auditada

- contratos, validação e canonicalização de `RunConfig`/`InputFrame` em `src/simulation/run`;
- estado inicial, tick explícito e executor headless atômico;
- inclusão explícita dos quatro estados `xoshiro128ss-v1` já aprovados em F0-04;
- layout binário `wwiirun.run-state.v1` e FNV-1a 64-bit sob demanda;
- corpus hardcoded e runners Node/Chromium em `tests/determinism` e `tests/e2e/run.determinism.spec.ts`;
- isolamento de metadados não competitivos e política de versionamento documentada na spec e no ADR-0005.

O worktree não fornece uma unidade versionada isolada de F0-05: F0-04 e F0-05 permanecem não rastreados sobre `HEAD` `95f9b5e`. Isso é finding de rastreabilidade do review do item, mas não produziu divergência nem violação do contrato determinístico nas fontes auditadas.

## Evidência executada

- `npm run check`: exit `0`; format, lint, typecheck, 89/89 unitários, 7/7 determinísticos e build passaram;
- `npm run test:unit:coverage -- --run tests/unit/run.test.ts`: exit `0`; 59/59 testes e `src/simulation/run` com 100% de statements, branches, functions e lines;
- `npm run test:determinism`: três execuções totais nesta revisão, todas com 7/7 testes e hashes idênticos;
- `CI=1 npm run test:e2e`: exit `0`; 6/6 testes nos projetos mobile e desktop, incluindo o mesmo corpus de run no Chromium;
- recomputação Python independente, sem importar o TypeScript, confirmou layouts de 392 bytes e hashes `0c8d1a30d7b17210`, `0de18a8817e3a594`, `6c361b31acf23fb3` e `8915f7da45a2a608` nos ticks 0, 1, 3 e 8;
- `npm ls --depth=0`: exit `0`; nenhuma dependência nova e árvore direta coerente com o manifesto/lockfile;
- `git diff --check`: exit `0`; `package-lock.json` sem alteração;
- varredura de `src/simulation`: nenhum uso de Phaser, DOM, rede, storage, locale, Web Crypto, `Math.random`, relógio, timer, delta variável ou enumeração incidental;
- inspeção confirmou 60 ticks/s, eixos inteiros quantizados, máscara de ações versionável, ordem normativa dos streams, overflow explícito e exclusão de locale/timezone/áudio/quality/viewport/FPS/cosmético do estado e hash.

## Cobertura do contrato

- config canônica, cópias defensivas, ordenação ASCII e isolamento entre runs: cobertos por unitários;
- limites de input, cópia de frame, tick único, overflow e atomicidade de lote: cobertos por unitários;
- equivalência frame/lote/partições vazias e repetição: coberta por unitários e corpus;
- pureza e cobertura explícita de todos os campos do layout/hash: cobertas por unitários e cálculo independente;
- paridade Node/Chromium e preservação dos vetores F0-04: cobertas pelos mesmos dados versionados;
- isolamento do core, ausência de entropia externa e de metadados não competitivos: confirmados por teste e inspeção estática.

## Checks ainda não aplicáveis

Entidades, colisão, spawn, score, distância e matemática escalada de gameplay entram em F1/F2. Serialização/restore e checkpoints persistidos pertencem a F0-08/F3-05. `ChallengeManifest`, períodos UTC/ISO e loadout normalizado pertencem a F3-03/F3-04. Firefox e WebKit permanecem adiados para o corpus de replay cross-browser. Essas ausências estão fora do escopo aprovado de F0-05 e não bloqueiam este veredito.

## Findings

Nenhum finding determinístico `Critical`, `High`, `Medium` ou `Low`.
