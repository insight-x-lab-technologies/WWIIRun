# Auditoria de determinismo — F0-04

Data: 2026-06-28  
Escopo: diff atual de `F0-04`, algoritmo `xoshiro128ss-v1`  
Veredito: `Pass`

## Ambiente

- Node `v24.15.0`;
- npm `11.12.1`;
- Vitest `4.1.9`;
- Playwright `1.61.1`, Chromium gerenciado pelo projeto.

## Superfície determinística auditada

- parser e divisão da seed em `src/simulation/random/seed.ts`;
- transição, clone e amostragem limitada em `src/simulation/random/xoshiro128ss.ts`;
- derivação dos quatro streams em `src/simulation/random/streams.ts`;
- corpus hardcoded e runners Node/Chromium em `tests/determinism` e `tests/e2e/random.determinism.spec.ts`;
- identificador, formato, tabela de streams e política de versionamento documentados na spec e no ADR-0004.

O diff relativo a `HEAD` mantém F0-04 separado das correções pendentes de F0-03. As mudanças de documentação da decisão `D-007` também aparecem no worktree, mas estão registradas como decisão aprovada do proprietário e não alteram runtime, consumo de RNG ou golden vectors.

## Evidência executada

- `npm run check`: exit `0`; format, lint, typecheck, 30/30 unitários, 5/5 determinísticos e build passaram;
- `npm run test:unit:coverage`: exit `0`; `src/simulation/random` mostrou 100% de statements, branches, functions e lines;
- `npm run test:unit:coverage -- --run tests/unit/random.test.ts --testTimeout=1000`: exit `0`; 29/29 testes em 605 ms, sendo 103 ms de testes, e cobertura random em 100%;
- `npm run test:determinism`: três execuções nesta revisão, todas com 5/5 testes e saídas golden idênticas;
- `CI=1 npm run test:e2e`: exit `0`; 4/4 testes nos projetos mobile e desktop, incluindo o mesmo corpus no Chromium;
- `npm ls --depth=0`: exit `0`; nenhuma dependência nova em F0-04 e árvore direta coerente com o lockfile;
- `git diff --check`: exit `0`; `package-lock.json` sem alteração;
- varredura de `src/simulation`: nenhum uso de Phaser, DOM, rede, storage, locale, crypto, `Math.random`, relógio ou timer; imports do módulo random são exclusivamente relativos e locais;
- inspeção da fonte normativa oficial confirmou o scrambler sobre `s[1]`, a transição 1.1, o salto equivalente a `2^64` chamadas e as constantes `8764000b/f542d2d3/6fa035c3/77f2db5b`.

## Cobertura do contrato

- seed canônica, erros tipados e recusa do estado zero: cobertos por unitários e corpus;
- transição unsigned, snapshots e sequência oficial: cobertos por unitários e goldens hardcoded;
- streams `spawn/loot/weather/patterns`, saltos `0..3` e independência de consumo: cobertos pelo corpus com duas seeds;
- rejection sampling, limites e consumo de descartes: cobertos por unitários e golden de estado final;
- paridade Node/Chromium: coberta pelo mesmo corpus versionado;
- isolamento do core e ausência de entropia externa: confirmados por inspeção estática.

## Checks ainda não aplicáveis

Loop fixo, inputs quantizados, hash/checkpoint, chunking de render e restore pertencem a F0-05. Challenge manifest e período UTC/ISO pertencem a F3-03. Isolamento de locale, áudio, quality tier e packs cosméticos sobre hashes pertence a F3/F6. Firefox e WebKit permanecem adiados para o corpus de replay cross-browser. Essas ausências não bloqueiam o escopo aprovado de F0-04.

## Findings

Nenhum finding `Critical`, `High`, `Medium` ou `Low`.
