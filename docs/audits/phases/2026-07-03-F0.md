# Auditoria de encerramento — F0

Data: 2026-07-03  
Fase: `F0 — Fundação e laboratório técnico`  
Commit auditado: `4440b7a` mais documentação de lifecycle não commitada  
Veredito: `Fail`

## Resumo

Os cinco critérios técnicos de saída da F0 têm evidência: shell instalável, golden headless repetível, CI verde, baselines físicos de celular/desktop e isolamento do core. F0-02 a F0-08 estão `Done` com aprovação independente registrada. A fase não pode fechar porque F0-01 está marcado `Done` sem spec e sem revisão independente, contrariando o lifecycle obrigatório aplicado pelo gate de fase.

## Inventário de itens e dependências

| Item | Dependência | Estado declarado | Spec/revisão | Resultado |
|---|---|---|---|---|
| F0-01 | — | `Done` | nenhuma spec ou aprovação localizada | `Block` |
| F0-02 | F0-01 | `Done` | spec; revisão aprovada em 2026-06-27 | `Pass` |
| F0-03 | F0-02 | `Done` | spec; quinta revisão `Approved` | `Pass` |
| F0-04 | F0-02 | `Done` | spec; segunda revisão aprovada | `Pass` |
| F0-05 | F0-04 | `Done` | spec; segunda revisão `Approved` | `Pass` |
| F0-06 | F0-02 | `Done` | spec; quarta revisão `Approved` | `Pass` |
| F0-07 | F0-03 | `Done` no worktree | spec; terceira revisão `Approved` | `Pass` com documentação ainda não commitada |
| F0-08 | F0-02 | `Done` | spec; segunda revisão `Approved` | `Pass` |

A pesquisa por `F0-01`, no índice de specs e no histórico desde o primeiro commit documental, encontrou apenas o item do roadmap e referências como dependência de F0-02. Não há objetivo, fora de escopo, critérios de aceitação, plano de testes ou aprovação independente próprios.

## Critérios de saída

| Critério | Evidência | Resultado |
|---|---|---|
| app vazio instalável | F0-07, build `/WWIIRun/`, inspector, Pages run `28687578396` e validação standalone/offline registrada | `Pass` |
| core headless repete golden run | 7/7 determinísticos no gate e em três repetições adicionais; relatório de determinismo atual | `Pass` |
| CI verde | workflow `Quality` run `28559326341`; Pages run `28687578396` no `HEAD` `4440b7a`, ambos registrados como `success` | `Pass` |
| baseline em celular e desktop | trios físicos versionados de iPhone 17 e desktop Windows, com hashes conferidos; ambos preservam resultado `fail` de performance | `Pass` para “medido”, não para PERF-01 |
| nenhuma API proibida no core | lint de fronteira, regressões e scan direto de `src/simulation` sem dependência proibida | `Pass` |

## Rastreabilidade de requisitos

- `AGENT-01` está `Done` e tem documentação operacional, templates, memória e uso efetivo do lifecycle;
- `DET-01`, `DET-02`, `PERF-01`, `ASSET-02`, `PWA-01`, `DIST-01`, `UI-04` e `COST-01` mantêm corretamente evidência parcial sem serem promovidos a `Done` antes das fases responsáveis;
- F0-04/F0-05 sustentam PRNG, tick/input/estado/hash; F0-08 sustenta a separação cosmética; F0-06 sustenta medição/budgets; F0-07 sustenta instalação/offline/subpath.

## Gates executados

| Comando/check | Resultado |
|---|---|
| `npm ci` / `npm ls --depth=0` | `Pass`; 460 pacotes e árvore direta coerente |
| `npm run check` | `Pass`; format, lint, dois typechecks, 256 unitários, 7 determinísticos, content, build, PWA inspector e budget |
| `npm run test:unit:coverage` | `Pass`; 256/256, random/run em 100% |
| `npm run test:determinism` ×3 adicionais | `Pass`; 7/7 em todas |
| build com `WWIIRUN_BASE_PATH=/WWIIRun/` | `Pass`; inspector para subpath e budgets verdes |
| `npm audit --offline` | `Pass`; 0 vulnerabilidades encontradas |
| hashes dos nove baselines datados | `Pass`; todos coincidem com o intake versionado |
| `git diff --check` | `Pass` |
| `npm run test:e2e` / `npm run test:pwa` frescos | sem resultado: loopback bloqueado no sandbox e executor externo ficou preso; não contado como passe |
| reconfirmação remota fresca | sem resultado: DNS bloqueado no sandbox e consulta autorizada externa ficou presa; evidência imutável já registrada foi usada |

## Gap bloqueante

### F0-PHASE-LIFECYCLE-01 — F0-01 sem spec/revisão

Severidade: `High`.

F0-01 foi concluído antes do restante do processo, mas o gate atual não permite exceção implícita: todo item da fase precisa de spec aprovada e revisão independente. Sem contrato próprio, não é possível provar de forma rastreável se documentação base, requisitos, ADRs, templates e memória satisfazem o escopo pretendido nem se o item pode permanecer `Done`.

Ação mínima: criar uma spec retrospectiva e verificável para F0-01, sem alterar runtime; mover o item ao estado coerente enquanto a lacuna estiver aberta; depois executar `$review-roadmap-item F0-01`. Somente então repetir `$audit-roadmap-phase F0`.

## Riscos residuais e deferimentos

- todos os trios físicos F0 falham ao menos frame p95; desktop/tablet também registram Long Tasks. F0 exigia baseline medido, não `PERF-01` aprovado; otimização deve ocorrer antes de alegação de 60 FPS;
- ADR-0007 aceita somente para os baselines F0 as 119 janelas/595 s, sem presumir os cinco segundos ausentes;
- desktop com GPU integrada, iPad, Firefox/WebKit e profiling do iPhone permanecem sem cobertura completa;
- FNV-1a detecta divergência, mas não autentica cliente; replay/manifest/backend pertencem a F3/F5;
- cache PWA continua sujeito a eviction e a instalação fora de Chromium é futura;
- o worktree já continha fechamento documental de F0-07 e `RTK.md` não rastreados/modificados. A auditoria preservou essas mudanças e adicionou somente os dois relatórios e o handoff desta sessão.

## Migração e rollback

Não há save, ruleset publicada, placar ou migração de dados a executar para esta auditoria. Os itens implementados registram rollback próprio; F0-07 requer worker de desativação antes de remover um service worker já publicado. Nenhum runtime, golden, baseline, threshold, asset ou workflow foi alterado.

## Próximo prompt exato

`$specify-roadmap-item F0-01 — crie uma spec retrospectiva verificável para a documentação base, requisitos, ADRs, templates e memória; preserve runtime e evidências existentes, defina critérios/testes documentais e deixe o item pronto para revisão independente.`
