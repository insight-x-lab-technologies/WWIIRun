# Auditoria de encerramento — F0

Data: 2026-07-04  
Fase: `F0 — Fundação e laboratório técnico`  
Commit auditado: `4440b7a` mais documentação de lifecycle no worktree  
Veredito: `Pass`

## Resumo

F0 pode fechar. Os oito itens estão `Done` com spec e revisão independente; a lacuna histórica de F0-01 identificada na auditoria de 2026-07-03 foi remediada e revisada. Os cinco critérios de saída e todos os gates aplicáveis possuem evidência fresca ou remota identificada.

## Inventário de itens

| Item | Dependência | Estado | Evidência de revisão | Resultado |
|---|---|---|---|---|
| F0-01 | — | `Done` | spec retrospectiva; revisão aprovada em 2026-07-04, 11/11 critérios | `Pass` |
| F0-02 | F0-01 | `Done` | revisão aprovada em 2026-06-27 | `Pass` |
| F0-03 | F0-02 | `Done` | quinta revisão aprovada; Actions run `28559326341` | `Pass` |
| F0-04 | F0-02 | `Done` | segunda revisão e auditoria determinística aprovadas | `Pass` |
| F0-05 | F0-04 | `Done` | segunda revisão e corpus Node/Chromium aprovados | `Pass` |
| F0-06 | F0-02 | `Done` | quarta revisão aprovada; baselines físicos preservados | `Pass` |
| F0-07 | F0-03 | `Done` | terceira revisão aprovada; Pages run `28687578396` | `Pass` |
| F0-08 | F0-02 | `Done` | segunda revisão aprovada | `Pass` |

Todas as dependências estão satisfeitas. Specs, índices, roadmap e requisitos mantêm o lifecycle e os requisitos amplos corretamente parciais quando pertencem a fases futuras.

## Critérios de saída

| Critério | Evidência | Resultado |
|---|---|---|
| app vazio instalável | F0-07; manifest/precache/subpath; PWA instalada e reload offline; PWA E2E fresco 10/10 | `Pass` |
| core headless repete golden run | auditoria determinística de 2026-07-04; 7/7 no gate e em três repetições; corpus Chromium fresco | `Pass` |
| CI verde | workflow `Quality` run `28559326341`; Pages run `28687578396` no SHA `4440b7a` | `Pass` |
| baseline em celular e desktop | trios versionados de iPhone 17 e desktop Windows, com hashes/evidência de revisão preservados | `Pass` para “medido” |
| nenhuma API proibida no core | lint de fronteira, regressões e scan direto sem imports/entropia proibidos | `Pass` |

Os baselines físicos preservam resultado de performance `fail`; o exit de F0 exige medição, não aprovação de `PERF-01`. O requisito permanece corretamente `Planned`.

## Gates executados

| Comando/check | Resultado |
|---|---|
| `npm ls --depth=0` | `Pass`; árvore direta coerente |
| `npm run check` | `Pass`; formato, lint, dois typechecks, 256 unitários, 7 determinísticos, validator, build, inspector e budget |
| `npm run test:unit:coverage` | `Pass`; 256/256; random/run em 100% |
| `npm run test:determinism` ×3 adicionais | `Pass`; 7/7 em cada execução |
| build com `WWIIRUN_BASE_PATH=/WWIIRun/` | `Pass`; inspector e budgets verdes |
| `CI=1 npm run test:e2e` | `Pass`; produto/determinismo 6/6 e harness 1/1 em Chromium |
| `CI=1 npm run test:pwa` | `Pass`; 10/10 nos dois viewports, incluindo offline/update A/B |
| `npm audit --offline` | `Pass`; nenhuma vulnerabilidade conhecida no cache local |
| `git diff --check` | `Pass` |
| auditoria determinística atual | `Pass`; sem findings |

## Rastreabilidade e restrições

- `AGENT-01` está `Done` com spec/revisão; `DET-01`, `DET-02`, `PERF-01`, `ASSET-02`, `PWA-01`, `DIST-01`, `UI-04` e `COST-01` mantêm evidência parcial sem promoção indevida;
- não há save publicado, placar, migração de dados ou ruleset pública a migrar;
- rollback dos itens está documentado; remover o service worker publicado exige o worker de desativação previsto por F0-07;
- nenhum runtime, golden, baseline, threshold, asset ou workflow foi alterado pela auditoria.

## Riscos residuais aceitos/deferidos

- os trios físicos F0 falham em frame p95; desktop/tablet também têm Long Tasks. Responsável: engenharia; destino: otimização e matriz representativa de F1/F7, sem afrouxar thresholds;
- ADR-0007 limita a exceção histórica a 119 janelas/595 s; o coletor futuro permanece em 120/600 s;
- desktop integrado, iPad, Firefox/WebKit e profiling Safari continuam sem cobertura completa; destino: matriz publicada de F7;
- FNV-1a detecta divergência, mas não autentica cliente; replay/manifest/backend pertencem a F3/F5;
- o worktree contém documentação aprovada ainda não commitada, inclusive a remediação F0-01 e estes relatórios. Isso não invalida a evidência, mas deve ser versionado como unidade documental antes de trabalho concorrente.

## Veredito e próximo prompt

`Pass`. F0 está encerrada e F1 pode iniciar pelo primeiro item.

Próximo prompt exato:

`$specify-roadmap-item F1-01 — defina a gameplay scene, viewport lógico e input teclado/touch conforme os exit criteria de F1, preservando o core determinístico e a separação simulation/game/app.`
